<div class="row">
  <div class="col-md-3"></div>
  <div class="col-md-8">
    <h2>Book detail</h2>
    <table>
      <tbody>
        <!--% @book.each do |book| %-->
          <tr>
            <!--%= image_tag @user.get_profile_image %-->
            <td><%= @book.title %></td>
            <td><%= @book.body %></td>
          </tr>
        <!--% end %-->
      </tbody>
    </table>
  </div>
</div>

