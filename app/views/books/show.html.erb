<h1>Book detail</h1>
<table>
  <tbody>
    <% @book.each do |book| %>
      <tr>
        <!--%= image_tag @user.get_profile_image %-->
        <td><%= book.title %></td>
        <td><%= book.body %></td>
      </tr>
    <% end %>
  </tbody>
</table>

